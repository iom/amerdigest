globalVariables(unique(c(
  # digest_data:
  "countrynames",
  # digest_plot:
  "countrynames",
  # get_regflow:
  "geo", "regin", "regout","n",
  # get_regnet:
  "geo", "n_in", "n_out", "regin", "regout","n",
  # plot_empty:
  "i",
  # plot_regflow:
  "var", "n",
  # plot_regnet:
   "n"
)))
