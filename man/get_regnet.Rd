% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_regnet.R
\name{get_regnet}
\alias{get_regnet}
\title{Prepare Regional Net Registration Data for Charts}
\usage{
get_regnet(iso = NULL)
}
\arguments{
\item{iso}{Optional character. ISO3 country code to filter the data by geography.
If NULL (default), returns data for all geographies.}
}
\value{
A data frame with columns \code{t}, \code{geo}, \code{n_in}, \code{n_out}, and \code{n} where \code{n} is net registrations.
}
\description{
Calculates net registrations by subtracting outflows from inflows aggregated by geography and time.
}
\examples{
# Get net registrations for all geographies
net_all <- get_regnet()

knitr::kable(head(net_all,5))
# Get net registrations for USA only

net_usa <- get_regnet("USA")

knitr::kable(head(net_usa,5))
}
